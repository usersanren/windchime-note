# 风铃便签 · 代码审核与优化建议

> 审核日期：2026-08-11　|　审核范围：desktop-note-widget 全部前端 + Rust 后端
> 本文档列出发现的问题与优化点，按优先级排序，每条给出位置、问题、影响、建议方案。

---

## 一、本次会话已完成的改动（背景）

1. **修复 AI 生成**：Rust 侧补齐 `generate_ai_quote` 命令 + reqwest 依赖（此前桌面端 AI 生成一直失效）
2. **撕边线随摆**：撕边线移入摆动体，按钮组保持固定（避免误点）
3. **撕边线位置**：改 absolute + 内联 `top` 定位，绕开 flex margin 与 Tailwind HMR 双重不确定性
4. **字体/行数设置**：新增 fontSize（12–28 可自定义）、maxLines（1–3 行）设置项
5. **收起动画**：新增收起按钮 + 卷起动画 + 顶部小圆点展开

---

## 二、问题清单（按优先级）

### 🔴 P0 — 需尽快处理

#### P0-1 自定义字号输入存在"输入被打断"问题
- **位置**：`SettingsPanel.tsx` 字体自定义输入框（`value={fontSize}` 受控 + onChange 即时 setFontSize）
- **问题**：输入框是受控组件，`onChange` 立即调用 `setFontSize` 并钳制 12–28。用户想输入「14」时：敲「1」→ 被钳制为 12 → 继续敲「4」→ 变成 124 → 钳制 28。**无法输入任意中间值**，只能靠挡位按钮。
- **影响**：自定义输入功能形同虚设，用户会困惑。
- **建议方案**：改为「本地草稿 + 失焦提交」模式——
  - 输入框用本地 `useState<string>` 草稿，`onChange` 只更新草稿不写 store
  - `onBlur` / 回车时校验并 `setFontSize`
  - 或输入框改为 `type="text"` + `inputMode="numeric"`，避免 number 控件自动钳制
- **工作量**：约 20 分钟

#### P0-2 AI API Key 明文存储
- **位置**：`persistence.ts` → Tauri Store（`widget-config.json`）/ localStorage
- **问题**：API Key 明文落盘在 `%APPDATA%`，无任何加密；一旦文件泄露 Key 即泄露。
- **影响**：安全隐患（用户自用场景风险可控，但应知晓）。
- **建议方案**（二选一）：
  - 最低成本：设置页加提示文案"Key 明文存储于本机，请勿在多用户机器使用"
  - 完整方案：接入 Windows Credential Manager（新增 Rust 命令读写凭据），本地存储仅存引用
- **工作量**：提示 10 分钟；Credential Manager 约 2–4 小时

---

### 🟠 P1 — 建议优先优化

#### P1-1 设置面板高度固定 400px，字体/行数新项加入后更拥挤
- **位置**：`SettingsPanel.tsx`（`max-h-[400px]`）
- **问题**：新增「字体」「显示行数」两个 Section 后内容变长，滚动条几乎必现；窗口 520×520 内可滚动区域有限。
- **建议方案**：面板高度自适应到窗口（`max-h-[calc(100vh-140px)]`）或改两列布局（左行为/主题，右显示/字体）；也可给面板加"紧凑模式"。
- **工作量**：30 分钟–1 小时

#### P1-2 拖动窗口 X 坐标未按 DPI/多显示器边界钳制到当前显示器
- **位置**：`useDragPosition.ts` + `App.tsx`（只取 `currentMonitor` 且 `position.y` 恒 0）
- **问题**：多显示器（不同分辨率/缩放）时，`getScreenSize()` 只读主显示器尺寸；窗口可被拖到副屏范围外，或缩放比例不同导致位置错乱。DEVELOPMENT_SUMMARY 也标注"未做多显示器位置记忆"。
- **建议方案**：拖拽时基于「窗口所在显示器」钳制（`currentMonitor()` 动态查询），并记忆位置时带上显示器标识；`y` 允许在 0 附近 ±20px 微调。
- **工作量**：2–3 小时

#### P1-3 鼠标穿透轮询常驻 40ms，窗口隐藏时仍在跑
- **位置**：`passthrough.rs`（40ms 循环）
- **问题**：窗口隐藏到托盘后，轮询线程仍持续获取光标位置并比对，浪费 CPU（虽已判断 `is_visible` 跳过切换，但循环未停）。
- **建议方案**：`is_visible()` 为 false 时 `thread::sleep` 拉长到 500ms–1s，或监听窗口显隐事件启停线程。
- **工作量**：30 分钟

#### P1-4 AI 生成失败无重试、无超时提示
- **位置**：`quoteService.ts` / `useQuoteManager.ts`（已知待办）
- **问题**：失败直接回退随机句，用户看不到失败原因持续存在（错误条 1 次即消失？实为刷新后消失）；浏览器降级路径无超时。
- **建议方案**：
  - AI 按钮失败后保留错误条 + 显示"重试"按钮
  - 浏览器降级路径加 `AbortController` 30s 超时
  - 失败时可自动重试 1 次再回退
- **工作量**：1–2 小时

#### P1-5 锁定（贴纸）模式无 UI 提示，解锁只能走托盘
- **位置**：`tray.rs` / `ControlButtons.tsx`（已知待办）
- **问题**：锁定后便签不可交互，新用户不知道如何解锁；托盘菜单项虽有"解除锁定"但不易发现。
- **建议方案**：锁定瞬间显示 2–3 秒浮动提示"已锁定到桌面，右键托盘图标解除锁定"（可配 `pointer-events-none` 提示条）；或锁定后卡片角落画个小锁标记。
- **工作量**：1 小时

---

### 🟡 P2 — 代码质量与维护性

#### P2-1 撕边线 `top: 278` 等魔法数字散落且无来源注释
- **位置**：`HangingWidget.tsx`（`ZONE` / `BUTTON_TOP` / `top: 278` / `ROPE_HEIGHT`）
- **问题**：位置全靠"经验值"，一旦 Rope 高度/卡片尺寸调整会全部错位，且无从追溯。
- **建议方案**：抽成布局常量对象并加推导注释：
  ```ts
  const LAYOUT = {
    rope: 92, knot: 12, knotOverlap: 2,
    card: { w: 400, h: 220 }, cardGap: 4,
    buttonTop: 314, tearTop: 278, // 卡片底 318 - 40（摆动体内容实际高度偏差）
  } as const;
  ```
  或干脆基于 ref 实测（`getBoundingClientRect`）动态计算撕边线位置。
- **工作量**：30 分钟–1 小时

#### P2-2 三个 Rust 命令是死代码
- **位置**：`commands.rs`（`set_window_opacity` / `toggle_window` / `quit_app`）、`tauri.ts`（`setWindowOpacity` / `quitApp` / `toggleWindow`）
- **问题**：透明度实际走前端 CSS opacity；显隐走托盘/快捷键（Rust 侧直接处理）；退出走托盘。前端从未调用这几个命令。
- **影响**：维护者误以为可用；`platform.rs` 的 LWA 分层窗口代码也因此闲置。
- **建议方案**：删除前端导出与 Rust 命令注册（保留 `platform::cursor_position` 供穿透用）；或明确标注 `#[deprecated]` 并保留作为能力备用。**注意**：删除需同步 `capabilities/default.json` 权限与 `lib.rs` 注册。
- **工作量**：30 分钟

#### P2-3 `desktop.rs` 未编译死代码保留
- **位置**：`src-tauri/src/desktop.rs`（已知：WorkerW 嵌入方案已放弃）
- **问题**：文件保留但未 `mod` 声明，无编译产物，纯占位。
- **建议方案**：文件头部注释已说明；建议移到 `src-tauri/archive/` 或删除（git 历史可找回）。
- **工作量**：10 分钟

#### P2-4 Rope 实际高度（含绳结）与 `ROPE_HEIGHT` 语义不符
- **位置**：`Rope.tsx`（绳子 92 + 绳结 12 - 重叠 2 = 实际 102px）
- **问题**：`ROPE_HEIGHT = 92` 仅指绳子本体，但容器实际占 102px，这是之前撕边线错位 10px 的根因之一；后续维护极易再次踩坑。
- **建议方案**：Rope 组件导出 `ROPE_TOTAL_HEIGHT` 常量，或让 Rope 接收总高自动拆解，注释写清。
- **工作量**：30 分钟

#### P2-5 物理摆动参数全部硬编码
- **位置**：`usePhysicsSwing.ts`（K=9、阻尼 0.3/0.2/0.26、冲量、限幅 15°）
- **问题**：手感参数集中但无文档/无配置，想微调需读源码。
- **建议方案**：抽成 `SWING_PARAMS` 常量对象放文件顶部 + 注释物理含义；未来可做成设置项。
- **工作量**：20 分钟

---

### 🟢 P3 — 体验与可选增强

| # | 项 | 说明 | 建议 |
|---|---|---|---|
| P3-1 | 收起动画期间可交互 | 收起 0.45s 内按钮仍可点 | `collapsed` 过渡期禁用交互或缩短动画 |
| P3-2 | 展开无回弹动画 | 从收起态展开直接回 state，无过渡趣味 | 展开时给一次 `pluck('release')` 余摆 |
| P3-3 | 无"关于/版本"信息 | 用户无法知道版本号、更新说明 | 设置页底部加版本号（读 tauri.conf） |
| P3-4 | 无自动更新 | 装机后只能手动重装 | 引入 `tauri-plugin-updater`（需更新服务器） |
| P3-5 | 无右键菜单 | 便签上右键无任何响应 | 可加右键：换句/主题快捷切换/锁定 |
| P3-6 | 无中英文切换 | 界面硬编码中文 | 需要多语言时引入 i18n（当前单用户可暂缓） |
| P3-7 | 卡片点击/拖动边界 | 4px 阈值区分，拖拽时松手有余摆 | 已良好；可加"双击卡片打开设置" |
| P3-8 | 每日更新无 UI 反馈 | 跨天后换句无提示 | 可加"今日新句"印章角标（复用 DailyStamp） |
| P3-9 | 无性能火焰图/日志 | 摆动手感调优靠体感 | 加 `console.debug` 开关或 rAF 帧率统计 |

---

## 三、本次新增功能的验收检查清单

- [x] 字体档位 14/16/18/20/22 + 自定义 12–28（**注：P0-1 输入被打断待修**）
- [x] 显示行数 1/2/3 行，长句省略号截断（`-webkit-line-clamp`）
- [x] 收起按钮 → 卷起动画（scaleY 0 + 上移 0.45s）→ 顶部小圆点
- [x] 点击圆点展开恢复
- [x] 收起后鼠标穿透只保留圆点区域（`interactiveRefs` 动态切换）
- [x] 锁定状态下强制展开 + 隐藏收起按钮
- [ ] 收起状态重启后恢复展开（默认，非持久化）——已符合预期

---

## 四、建议的下一批迭代（若采纳）

**快速修复包（半天）**：P0-1 输入框、P0-2 存储提示、P1-1 面板布局、P1-3 轮询降频
**体验包（1 天）**：P1-4 AI 重试、P1-5 锁定提示、P3-2 展开回弹
**工程包（1 天）**：P2-1 布局常量、P2-2 死代码清理、P2-4 Rope 高度、P2-5 摆动参数
**可选长线**：P1-2 多显示器、P3-4 自动更新、P3-5 右键菜单

---

*审核基于当前源码（前端 21 文件 + Rust 6 模块）逐文件阅读完成；未运行编译（360 拦截，编译命令见 DEVELOPMENT_SUMMARY）。*
